# Build context hygiene. node_modules especially: the image installs its own
# (Linux) tree, and copying a macOS one over it produces native-module errors
# that only appear at request time.
**/node_modules
**/.venv
**/__pycache__
**/.pytest_cache
frontend/.next
frontend/out
.git
.env
.env.local
**/*.log
